FOUNDRY_PROJECT_ENDPOINT=https://<YOUR-RESOURCE>.services.ai.azure.com/api/projects/<YOUR-PROJECT>
AZURE_AI_MODEL_DEPLOYMENT_NAME=<YOUR-MODEL-NAME>
APPLICATIONINSIGHTS_CONNECTION_STRING=
ENABLE_OTEL_CONTENT_RECORDING=false
ENABLE_SENSITIVE_DATA=false

# Local orchestration uses the real Foundry model by default.
ORCHESTRATION_BACKEND=local
LOCAL_ORCHESTRATOR_RESPONSES_URL=http://localhost:8088/responses

# Backend options:
# - local: FastAPI runs the WorkflowBuilder chain in-process.
# - local_responses: FastAPI calls a local ResponsesHostServer at LOCAL_ORCHESTRATOR_RESPONSES_URL.
# - hosted: FastAPI calls deployed Foundry hosted agents by name.
ORCHESTRATOR_AGENT_NAME=municipal-incident-orchestrator

# Child agent mode used by the hosted orchestrator:
# - framework: call the hosted Microsoft Agent Framework child agents.
# - prompt: call the Foundry prompt/declarative child agents ending in -dcl.
# - custom: use INTAKE_AGENT_NAME, ROUTING_AGENT_NAME, and NOTIFICATION_AGENT_NAME below.
CHILD_AGENT_MODE=framework
INTAKE_AGENT_NAME=municipal-incident-intake
ROUTING_AGENT_NAME=municipal-incident-routing
NOTIFICATION_AGENT_NAME=municipal-incident-notification
FOUNDRY_TOKEN_SCOPE=https://ai.azure.com/.default

INCIDENT_API_URL=http://localhost:8000
API_HOST=0.0.0.0
API_PORT=8000
STREAMLIT_SERVER_PORT=8501